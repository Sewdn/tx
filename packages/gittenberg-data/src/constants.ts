/**
 * Bump when persisted entity shapes change (new required fields, renames).
 * Using a new namespace avoids decode errors from older localStorage / sqlite snapshots.
 */
export const GITTENBERG_DEFAULT_STORAGE_NAMESPACE = "gittenberg-prototype-v2"
